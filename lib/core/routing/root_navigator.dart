import 'package:flutter/material.dart';

/// Root [NavigatorState] for AutoRoute and Sentry (e.g. [SentryFeedbackWidget]).
final GlobalKey<NavigatorState> rootNavigatorKey = GlobalKey<NavigatorState>();
