// Animation and timing constants

const Duration kAnimFast = Duration(milliseconds: 200);
const Duration kAnimDefault = Duration(milliseconds: 300);
const Duration kAnimSkeleton = Duration(milliseconds: 1500);
const Duration kAnimDebounce = Duration(milliseconds: 250);
const Duration kUiDelayShort = Duration(milliseconds: 500);
