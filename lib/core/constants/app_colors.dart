import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFF005B8C);

  /// Saturated red for white icons on [GlassIconBadge] (matches visual weight of primary-tinted badges).
  static const Color destructiveIconBadgeTint = Color(0xFFC62828);
  static const Color white = Colors.white;
  static const Color black = Colors.black;
  static const Color grey = Colors.grey;
}
